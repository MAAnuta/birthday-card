# Birthday Fireworks Extension

Поздравительное расширение для VS Code/Cursor, которое показывает фейерверк с надписью "С днем рождения!" при открытии файлов.

## Особенности

- ✨ Красивый фейерверк с разноцветными частицами
- 🎉 Надпись "С днем рождения!" с эффектом свечения
- 🚀 Автоматическое появление при открытии любого файла
- ⏰ Автоматическое закрытие через 8 секунд
- 🎨 Темный фон для лучшего эффекта

## Установка

1. Скачайте или клонируйте этот репозиторий
2. Установите зависимости:
   ```bash
   npm install
   ```
3. Скомпилируйте расширение:
   ```bash
   npm run compile
   ```
4. Установите расширение в VS Code:
   - Откройте VS Code
   - Нажмите `Ctrl+Shift+P` (или `Cmd+Shift+P` на Mac)
   - Введите "Extensions: Install from VSIX"
   - Выберите файл `birthday-fireworks-1.0.0.vsix` из папки проекта

## Разработка

Для запуска в режиме разработки:

1. Откройте папку расширения в VS Code
2. Нажмите `F5` для запуска отладки
3. В новом окне Extension Development Host откройте любой файл

## Как это работает

Расширение:
- Активируется при запуске VS Code
- Слушает события открытия файлов
- Создает WebView панель с анимацией фейерверка
- Автоматически закрывает панель через 8 секунд

## Структура файлов

```
birthday-fireworks/
├── src/
│   └── extension.ts       # Основной код расширения
├── package.json           # Конфигурация расширения
├── tsconfig.json          # Конфигурация TypeScript
└── README.md             # Документация
```

## Настройка

Расширение работает автоматически после установки. Фейерверк появляется:
- При открытии существующих файлов
- При первом запуске (если файл уже открыт)

## Требования

- VS Code 1.74.0 или выше
- Node.js 16.x или выше

## Лицензия

MIT
